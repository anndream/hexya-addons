<hexya>
    <data>

    <view id="product_product_template_tree_view" model="ProductTemplate">
            <tree string="Product">
                <field name="Sequence" widget="handle"/>
                <field name="DefaultCode"/>
                <field name="Name"/>
                <field name="ListPrice"/>
                <field name="StandardPrice"/>
                <field name="CategId"/>
                <field name="Type"/>
                <field name="UomId" options="{'no_open': True, 'no_create': True}" groups="product.group_uom"/>
                <field name="Active" invisible="1"/>
            </tree>
    </view>

    <view id="product_product_template_only_form_view" model="ProductTemplate">
        <form>
            <xpath expr="//form" position="attributes">
                <attribute name="name">Product Template</attribute>
            </xpath>
                <field name="DefaultCode" attrs="{'invisible': [('product_variant_count', '>', 1)]}"/>
                <field name="Barcode" attrs="{'invisible': [('product_variant_count', '>', 1)]}"/>
            <div name="button_box" position="inside">
                <button name="%(product.product_variant_action)d" type="action"
                        icon="fa-sitemap" class="oe_stat_button"
                        attrs="{'invisible': [('product_variant_count', '&lt;=', 1)]}"
                        groups="product.group_product_variant">
                    <field string="Variants" name="product_variant_count" widget="statinfo" />
                </button>
            </div>
            <xpath expr="//page[@name='sales']" position="after">
                <page name="variants" string="Variants" groups="product.group_product_variant">
                    <field name="AttributeLineIds" widget="one2many_list" context="{'show_attribute': False}">
                        <tree string="Variants" editable="bottom">
                            <!--<field name="AttributeId"/>-->
                            <!--<field name="ValueIds" widget="many2many_tags" options="{'no_create_edit': True}" domain="[('attribute_id', '=', attribute_id)]" context="{'default_attribute_id': attribute_id}"/>-->
                        </tree>
                    </field>
                    <p class="oe_grey">
                        <strong>Warning</strong>: adding or deleting attributes
                        will delete and recreate existing variants and lead
                        to the loss of their possible customizations.
                    </p>
                </page>
            </xpath>
        </form>
    </view>


        <action id="product_product_template_action_form" type="ir.actions.act_window" model="ProductTemplate"
                name="Product Templates" view_mode="tree,form" search_view_id="product_product_template_search_view"
                view_id="product_product_template_list_view"/>

    </data>
</hexya>
